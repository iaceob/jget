<?xml version="1.0" encoding="utf-8"?>
<slxlme>
    <container name="Cli">
        <sql id="registerCli">
            insert into j_cli(id, name, ip, heartbeat) values (?,?,?,now())
        </sql>
        <sql id="existCli">
            select id from j_cli where name=?
        </sql>
        <sql id="heartbeatCli">
            update j_cli set heartbeat=now() where id=?
        </sql>
    </container>
</slxlme>